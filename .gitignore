node_modules/
app/node_modules/
app/dist/
app/.expo/
.expo/
# build/deploy output (regenerated by `expo export`)
/scrump/
# private strategy doc — not for the public repo
OPPORTUNITY.md
# raw Falling Fruit export — contains street addresses, kept out of the public repo
data_raw.csv
.DS_Store
*.log

.claude/

app/.env
.env.local
# local analytics secrets (PostHog query key) — never commit
.analytics.env
